name: tabby
description: "Tabs for Bottom Nav Bar or Nav Rail depending on width."
version: 2.0.0
repository: https://github.com/ArcaneArts/tabby
environment:
  sdk: '>=3.3.3 <4.0.0'
  flutter: ">=1.17.0"
dependencies:
  flutter:
    sdk: flutter
  toxic: ^1.3.1
  copy_with_material: ^1.0.0
  lazy_load_indexed_stack: ^1.1.0
  gap: ^3.0.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
flutter: